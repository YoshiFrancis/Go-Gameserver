package leaderserver

/*
package wsserver

func (c *Client) handlePrompt(message string) {
	if c.prompt == USERNAME {
		c.username = message
		c.prompt = NONE
		c.room.server.clients[c.username] = c
		c.room.register <- c
	}
}

*/
